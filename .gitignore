# Dependency Directories
node_modules/
/dist
/build

# Expo Specific
.expo/
*.log
*.tmp

# React Native Caches
/android/build
/android/.gradle
/ios/build

# Metro Bundler Cache
.metro-cache/

# Editor/OS Files
.DS_Store
Thumbs.db
.vscode/

# Logs and Temp Files
*.log
*.txt
/tmp

# Security/Secrets (For local development environments)
.env
.env.local

# TypeScript (if applicable, though Expo usually handles this via Babel/Metro)
/coverage
/temp

# MongoDB local data (If running server locally, though usually kept separate)
# In a full-stack mobile context, this is often omitted unless the repo contains the server code too.

# Artifacts from npm/yarn scripts
/npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Artifacts from testing frameworks (e.g., Vitest/Playwright if used)
/test-results/
